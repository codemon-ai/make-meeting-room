# 그룹웨어 로그인 정보
GW_USER_ID=your_user_id
GW_PASSWORD=your_password

# Slack Bot (예정)
# https://api.slack.com/apps 에서 앱 생성 후 설정
# SLACK_BOT_TOKEN=xoxb-your-bot-token
# SLACK_SIGNING_SECRET=your-signing-secret
# SLACK_APP_TOKEN=xapp-your-app-token

# Google Calendar (Service Account + Domain-wide Delegation)
# 1. Google Cloud Console에서 프로젝트 생성
# 2. Google Calendar API 활성화
# 3. Service Account 생성 → JSON 키 다운로드
# 4. Google Admin Console에서 도메인 전체 위임 설정
#    - 클라이언트 ID: Service Account의 고유 ID (숫자)
#    - 범위: https://www.googleapis.com/auth/calendar
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project-id.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
GOOGLE_CALENDAR_USER=admin@your-domain.com  # impersonation 대상 (도메인 관리자)
