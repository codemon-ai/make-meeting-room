# 그룹웨어 로그인 정보
GW_USER_ID=your_user_id
GW_PASSWORD=your_password

# 그룹웨어 사용자 정보 (예약 기능 사용 시 필요)
# 로그인 후 개발자도구에서 확인 가능
# GW_EMP_SEQ=your_emp_seq
# GW_EMP_NAME=your_name
# GW_DEPT_SEQ=your_dept_seq
# GW_DEPT_NAME=your_dept_name
# GW_SUPER_KEY=rsquare|1000|dept_seq|emp_seq|u

# Slack Bot (Slack 서버 모드용)
# https://api.slack.com/apps 에서 앱 생성 후 설정
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_SIGNING_SECRET=your-signing-secret
SLACK_APP_TOKEN=xapp-your-app-token

# Google Calendar (선택사항)
# Google Cloud Console에서 OAuth2 클라이언트 생성 후 설정
# GOOGLE_CLIENT_ID=your_client_id
# GOOGLE_CLIENT_SECRET=your_client_secret
# GOOGLE_REDIRECT_URI=http://localhost:3000/oauth2callback
